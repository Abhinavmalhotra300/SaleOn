<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.skgroup.saleon">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
            android:allowBackup="false"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".activity.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.loginsignup.LoginRegister"
                  android:theme="@style/AppTheme.FullScreen"/>
        <activity android:name=".activity.loginsignup.Login"/>
        <activity android:name=".activity.loginsignup.SignUp"/>
        <activity android:name=".activity.loginsignup.ForgetPassword"/>
        <activity android:name=".activity.HomeScreen"/>
        <activity android:name=".activity.ItemList"/>
        <activity android:name=".activity.ItemDetail"/>
    </application>
</manifest>